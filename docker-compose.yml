services:
  backend:
    build: ./project/lcs-server
    ports:
      - "8080:8080"
    environment:
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  frontend:
    build: ./project/lcs-react
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: unless-stopped
